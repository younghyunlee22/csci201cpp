For this assignment you will modify your burger class from M01 Programming Assignment 2. You will add two static maps to the class for each enum. One map will map the enum value to an output string. The second will map a case insensitive string to an enum value. In your main create a vector of burgers and set up a loop that allows the user to order multiple burgers instead of just one like in the original assignment. Change the way the user enters their choices so they can enter strings instead of numbers.